rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas básicas: Solo requiere autenticación
    // Todos los usuarios autenticados pueden leer y escribir en todas las colecciones
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
